;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname 21B) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ())))
(require picturing-programs)

; 21.5.2

(define-struct person (first last age))

(define p1 (make-person "Aaron" "Sanders" 17))
(define p2 (make-person "Aaron" "Ledski" 17))
(define p3 (make-person "Bob" "Marly"

; 21.4.5

; 21.5.3

; 21.5.4